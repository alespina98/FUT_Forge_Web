import { directoryMetadata, renderDirectoryPage } from "@/lib/fc27/entity-pages";
export const metadata = directoryMetadata("nations");
export default function Page(){return renderDirectoryPage("nations")}

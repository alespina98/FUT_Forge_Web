import { directoryMetadata, renderDirectoryPage } from "@/lib/fc27/entity-pages";
export const metadata = directoryMetadata("leagues");
export default function Page(){return renderDirectoryPage("leagues")}

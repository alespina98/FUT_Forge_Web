import { directoryMetadata, renderDirectoryPage } from "@/lib/fc27/entity-pages";
export const metadata = directoryMetadata("clubs");
export default function Page(){return renderDirectoryPage("clubs")}

"use client";
import { useClerk, useUser } from "@clerk/nextjs";
import { useEffect,useState } from "react";
import { useRouter } from "next/navigation";
type Profile={username:string|null;email:string|null;role:"USER"|"ADMIN";tier:"FREE"|"PREMIUM"};
export function ClerkAccountPanel(){const {user}=useUser();const {signOut}=useClerk();const router=useRouter();const [profile,setProfile]=useState<Profile|null>(null);useEffect(()=>{fetch("/api/auth/profile").then(r=>r.ok?r.json():null).then(setProfile)},[]);return <div className="mx-auto max-w-md"><p className="section-label">FUT FORGE ACCOUNT</p><h1 className="mt-5 text-3xl font-semibold tracking-[-.04em]">Account</h1><div className="glass mt-8 flex flex-col gap-5 rounded-2xl p-6 sm:p-8"><div><p className="text-xs uppercase text-white/40">Username</p><p className="mt-1 text-sm">{profile?.username||user?.username||"—"}</p></div><div><p className="text-xs uppercase text-white/40">Email</p><p className="mt-1 text-sm">{profile?.email||user?.primaryEmailAddress?.emailAddress}</p></div><div><p className="text-xs uppercase text-white/40">Tier</p><p className="mt-1 text-sm">{profile?.tier||"FREE"}</p></div><button className="button-secondary self-start" onClick={async()=>{await signOut();router.push("/")}}>Log out</button></div></div>}
